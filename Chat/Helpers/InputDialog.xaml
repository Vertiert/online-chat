<Window x:Class="ChatApp.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="150" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="QuestionText" Grid.Row="0" Margin="0,0,0,10"/>
        <TextBox x:Name="AnswerTextBox" Grid.Row="1" Height="25" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Margin="0,0,10,0" IsDefault="True" Click="OKButton_Click"/>
            <Button Content="Отмена" Width="70" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>